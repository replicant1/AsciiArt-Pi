# Saved eval runs. Useful locally for comparing one prompt against another;
# not worth versioning, since each is a timestamped snapshot of a stochastic
# component rather than a fact about the code.
runs/
.DS_Store
